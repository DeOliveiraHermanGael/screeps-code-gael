/*
 * Module code goes here. Use 'module.exports' to export things:
 * module.exports.thing = 'a thing';
 *
 * You can import it from another modules like this:
 * var mod = require('Universal.logistics');
 * mod.thing == 'a thing'; // true
 */

module.exports = {
    Universal_sources: function() {
        if (Memory.updateSources) {
            Memory.sources = Memory.rooms.map(function(room) {
                for (i = 0; i < )
                return room.sources.map
                
            })
        }
    }
};